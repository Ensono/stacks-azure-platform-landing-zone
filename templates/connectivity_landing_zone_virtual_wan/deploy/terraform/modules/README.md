# Internal Modules

This directory contains internal child modules consumed by the root module. These modules are not intended for standalone use and should only be invoked from the root terraform configuration.

README.md files will be automatically generated for each child module using `terraform-docs`.
