# Config Templating

This module processes configuration templates and generates custom replacements for resource naming, location mappings, and default values used throughout the Platform Landing Zone deployment.
